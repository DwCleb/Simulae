import Config from 'react-native-config';

export const config = {
  i18n: {
    url: Config.I18N_URL,
    tokenEN: Config.I18N_EN_TOKEN,
    tokenPT: Config.I18N_PT_TOKEN,
  },
  baseURL: Config.BASE_URL,
};

export default config;
